import styled from "styled-components";

const LearnerFontSettingSelectionContainer = styled.div`
  margin: 0 auto;
  max-width: fit-content;

  & > * + * {
    margin-top: 1rem;
  }
`;

export default LearnerFontSettingSelectionContainer;
